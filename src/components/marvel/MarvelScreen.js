import React from 'react';
import {HeroList} from "../hero/HeroList";

const MarvelScreen = () => {
    return (
        <div>
            <HeroList/>
        </div>
    );
};

export default MarvelScreen;
